package src.monster;

import src.entity.Entity;
import src.main.GamePanel;

public class Monster extends Entity {
  public Monster(GamePanel gp) {
      super(gp);
      //this.worldX = startX;
      //this.worldY = startY;
      // Initialize other monster attributes (e.g., health, speed)
  }
  
  public void update() {
      // Add movement or AI behavior
  }
}

